!!!
%html
  %head
    %title= t('common.title')
    = stylesheet_link_tag "application", :media => "all"
    = javascript_include_tag "application" 
    = csrf_meta_tags
  %body{ :data => { :user => current_user.try(:to_json), :contents => @page_contents.try(:to_json) } }
    .navbar.navbar-fixed-top.navbar-inverse
      .navbar-inner
        .container
          %a.brand{ :href => '#' }= t('common.title')
    .container
      = yield
